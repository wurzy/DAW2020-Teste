extends layout

block content
  .w3-card-4
    .w3-container.w3-blue
        h1 Código da classe: #{dados.codigo}
    p(style="text-indent: 20px")
       a(href="/") Back  
    ul.w3-ul
     li 
      b Nível: 
      | #{dados.nivel}
     li 
      b Título: 
      | #{dados.titulo}
     li 
      b Descrição: 
      | #{dados.descricao}
     li
      b Estado: 
      |  #{dados.status}

    .w3-card-4
     .w3-container.w3-blue
      h1 Descendência da classe
    if dados.filhos.length > 0
     table.w3-table-all
      tr
        th Código
        th Título  
      each filho in dados.filhos
        tr
          td
            a(href='/codigo/' + filho.codigo)= filho.codigo
          td= filho.titulo
    else 
     ul.w3-ul
      li Não existente.

    if dados.nivel == 3
     .w3-card-4
      .w3-container.w3-blue
       h1 Processos Relacionados
     table.w3-table-all
      tr
       th Código
       th Título 
      each proc in dados.processosRelacionados
       if proc.idRel == "eCruzadoCom" || proc.idRel == "eComplementarDe" || proc.idRel == "eSuplementoDe" || proc.idRel == "eSuplementoPara"
         tr
          td
           a(href='/codigo/' + proc.codigo)= proc.codigo
          td= proc.titulo

  .w3-container.w3-blue
     address Generated by App-Server A84464 - DAW2020, 20 Jan 2021